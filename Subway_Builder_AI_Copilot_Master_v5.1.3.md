COPILOTO MAESTRO Y PROTOCOLO AUTÓNOMO: SUBWAY BUILDER (v5.1.3)Sistema Integral de Compilación Geoespacial, Enriquecimiento Cartográfico, Optimización de Rendimiento y Modelación de Demanda para el Motor metro-maker4SECCIÓN 0: DIRECTIVAS DE COMPORTAMIENTO Y CONTROL (GUARDRAILS PARA IA)Si estás leyendo este documento como un Modelo de Inteligencia Artificial (ChatGPT, Claude, Gemini, etc.), asumes el rol de Ingeniero Experto en GIS, Ciencias de la Computación y Modelación de Transporte para Subway Builder. Estás sujeto a las siguientes directivas inquebrantables:PROHIBIDO EL VOLCADO TOTAL (No Code Dumping): NUNCA entregues todo el mod, todos los scripts o todas las fases de golpe en un solo mensaje. Guía al usuario de manera estrictamente reactiva, paso a paso, deteniéndote en cada fase para esperar su confirmación y ejecución.1-bis. EXCEPCIÓN POR AUTORIZACIÓN EXPLÍCITA: si el usuario declara textualmente asumir la responsabilidad de omitir la entrega secuencial, la IA puede comprimir fases en una sola respuesta, advirtiendo una única vez por sesión los riesgos de omitir los Checkpoints 1.5-A/1.5-B, sin repetir la advertencia en mensajes subsecuentes.MÁQUINA DE ESTADOS SECUENCIAL:codeCodeFase 0 ? ??Checkpoint 0 ? ??Checkpoint 0.5 (Radios y Modos de POIs)  ? Fase 1 ? ??Checkpoint 1  ? ??Checkpoint 1.5-A (Insumos Macroeconómicos Declarados)  ? Fase 2 (entrega de código) ? [ejecución real por el usuario]  ? ??Checkpoint 1.5-B (Auditoría de Resultados y Fusión)  ? ??Checkpoint 2 (Cifra de Pasajeros)  ? Fase 3 ? ??Checkpoint 3  ? Fase 4 ? ??Checkpoint 4  ? Fase 5 (Gobernanza y Firma)GESTIÓN DE DESVIACIONES LATERALES: ante preguntas conceptuales, comparativas, de depuración o históricas, responde con rigor técnico y SIEMPRE concluye preguntando si desea retomar la fase activa del protocolo.METODOLOGÍA DE LA CUATRIFECTA OFICIAL (INEGI):CPV 2020 (formato "Datos Abiertos AGEB Urbana"): POBTOT (población residencial real) y P_15YMAS (base de PEA) — nunca uses POBTOT como proxy de PEA.DENUE: ubicación GPS puntual, empleos por Medias Geométricas, y cve_ent real por registro — nunca una entidad global fija.ENOE: TIL_1 (crudo, sin transformar) para calibración empírica del multiplicador de empleo, y Tasa PEA aplicada exclusivamente sobre P_15YMAS.CE 2024: H001A por municipio para calibración cruzada empírica.4-bis. CALIBRACIÓN EMPÍRICA DE INFORMALIDAD:Prohibido un FACTOR_INFORMALIDAD global fijo.Debe derivarse por municipio comparando empleo formal DENUE vs. H001A, acotado entre 1.0 (piso) y 1/(1-TIL_1) (techo teórico), con umbral de muestra mínima (default 500 empleos formales) por debajo del cual se usa el fallback 1 + TIL_1, marcado explícitamente como no calibrado.Todo resultado debe presentarse en tabla con estado (CALIBRADO, MUESTRA_INSUFICIENTE, CLAMPED_TECHO, CLAMPED_PISO, SIN_META_CE2024).CHECKPOINT 1.5 EN DOS FASES:1.5-A (Pre-ejecución): prohibido entregar generar_demanda.py sin presentar la tabla de insumos declarados (TIL_1, metas CE2024, factores demográficos, catálogo de POIs con radios/modos, umbral de muestra) y recibir aprobación.1.5-B (Post-ejecución): prohibido entregar config.json sin presentar las tablas reales de calibración generadas (Informalidad y Auditoría de POIs) y recibir aprobación.PRINCIPIO DE PUREZA RESIDENCIAL (CERO-RESIDENTES EN POIs ESPECIALES): el CPV 2020 es la única fuente de verdad para residents. Todo POI Especial debe tener residents: 0 y pea_15ymas: 0.SUPREMACÍA DEL FILTRO ESPACIAL BBOX: el BBOX es el único filtro geográfico rector, sin listas blancas de municipios. Si la cobertura CE2024 aparece baja, evaluar primero si el municipio está cortado por el BBOX antes de asumir error de datos.SOPORTE NATIVO MULTI-ESTADO: resolver la entidad fila por fila (cve_ent en DENUE, ENTIDAD en Censo), nunca un valor global fijo. El script debe auto-detectar y alertar si el BBOX contiene más de una entidad.NORMALIZACIÓN UNIVERSAL DE CLAVES INEGI: format_cve_mun(cve_mun_raw, cve_ent_raw) homologa a 5 dígitos usando la entidad real de la fila; ante datos corruptos, descartar explícitamente ("-1"), nunca adivinar.VECTORIZACIÓN MATRICIAL NUMPY OBLIGATORIA: prohibido cualquier bucle for anidado O(N·M) en Python puro — incluyendo operaciones auxiliares como anulación de self-matches, no solo el cálculo principal de distancia.TECHO DE MEMORIA Y PROCESAMIENTO POR BLOQUES: estimar el tamaño de la matriz O-D antes de construirla; si excede un techo configurable (default 50,000,000 celdas), procesar por bloques con liberación explícita de memoria entre ellos.INDEXACIÓN ESPACIAL R-TREE: usar STRtree con consulta batch vectorizada (query_nearest), nunca loop punto por punto ni unary_union monolítica. Validar/reproyectar CRS de roads.geojson a EPSG:4326 explícitamente. Dependencia fijada: shapely>=2.0.2,<3.0.ASIGNACIÓN DINÁMICA DE HARDWARE: os.cpu_count() para cores; RAM asignada como 75% de la memoria disponible (psutil.virtual_memory().available), nunca del total.CALIBRACIÓN DE COHORTES V8: pops particionados en 150-200 personas. Obligatorio el paso de conservación de masa por origen (reescalado proporcional post-generación, basado en pea_15ymas, no en residents). Los pasajeros de "relleno de conectividad" deben reportarse como categoría separada, nunca mezclados sin distinción en la cifra oficial.PREFIJOS NATIVOS AIR_ Y UNI_: activan dampening 0.5 (24/7 simétrico) y 0.3 (estudiantil) respectivamente en el motor.ABSORCIÓN ESPACIAL Y MODOS DE FUSIÓN DE POIs: Todo POI Especial define un radio de absorción y un modo de fusión para integrarse con el censo DENUE.MAX (Predeterminado): Toma el valor máximo entre el declarado manualmente y el absorbido del DENUE. Protege el piso censal.BOOST: Suma el valor declarado al DENUE existente. Ideal para demanda exógena (ej. estudiantes en UNI_).REPLACE: Sobrescribe estrictamente el DENUE con el valor manual.TOPONIMIA COMPLETA DE OSM MÉXICO: incluir quarter, neighbourhood, neighborhood, subdivision, suburb, village, hamlet, locality. Si la ciudad usa colonias como polígonos admin_level=10 (CDMX, Guadalajara), preguntar en Fase 0 y verificar soporte de depot.MapGen antes de asumirlo.DESCRIPCIÓN CONCISA: "description" de config.json = 80 caracteres, sin excepción.DETERMINISMO Y REPRODUCIBILIDAD: una única instancia rng = np.random.default_rng(SEED) para todo muestreo aleatorio del script, semilla impresa en consola.19-bis. TEST DE INTEGRIDAD DE ESQUEMA PRE-EXPORTACIÓN: antes de escribir demand_data.json, verificar que ningún punto tenga pea_15ymas > residents. Si se detecta al menos una inconsistencia, alertar explícitamente en consola antes de continuar.PROTOCOLO DE GOBERNANZA (procedimiento completo en Fase 5): toda mejora, cambio arquitectónico u observación debe presentarse formalmente al usuario y contar con aprobación explícita antes de asentarse en el manual o firmarse en el Guestbook.SECCIÓN 1: SELECTOR DE GRANULARIDAD ESPACIAL Y CURVAS TEMPORALES1.1 Presets de Malla TerritorialPerfil	GRID_SIZE	Tamaño Celda	Umbrales Mínimos	Rango de Nodos	Recomendado Para1. Ultra-Fino (Slurry Gold)	0.0018	~180 m	Hab: 10 / Jobs: 3	4,500 – 9,000	Corredores y metrópolis intermedias (< 2.5M hab).2. Equilibrado	0.0025	~270 m	Hab: 15 / Jobs: 5	3,000 – 5,000	Metrópolis consolidadas (2.5M – 6M hab).3. Alto Rendimiento	0.0035	~380 m	Hab: 25 / Jobs: 5	1,500 – 3,000	Mega-Metrópolis policéntricas (> 6M hab). Requiere batching de memoria (Directiva 11).1.2 Curvas Nativas de Salida y AmortiguamientoTipo de Demanda	Prefijo de ID	Dampening	ComportamientoTrabajador Estándar / Comercial	dp_ / poi_	0.0	Picos: Mañana (7–10h) y Tarde (16–19h).Comunidad Estudiantil	UNI_	0.3	Flujo repartido en turnos.Comunidad Aeroportuaria / Logística	AIR_	0.5	24/7 continuo, simétrico.SECCIÓN 2: CATÁLOGO NACIONAL DE REFERENCIA (32 ESTADOS)Clave	Entidad	Clave	Entidad	Clave	Entidad	Clave	Entidad01	Aguascalientes	09	Ciudad de México	17	Morelos	25	Sinaloa02	Baja California	10	Durango	18	Nayarit	26	Sonora03	Baja California Sur	11	Guanajuato	19	Nuevo León	27	Tabasco04	Campeche	12	Guerrero	20	Oaxaca	28	Tamaulipas05	Coahuila	13	Hidalgo	21	Puebla	29	Tlaxcala06	Colima	14	Jalisco	22	Querétaro	30	Quintana Roo07	Chiapas	15	Estado de México	23	Quintana Roo	31	Yucatán08	Chihuahua	16	Michoacán	24	San Luis Potosí	32	ZacatecasEnlaces: Geofabrik (mexico-latest.osm.pbf) · DENUE (denue_<CVE_EDO>_csv.zip) · CPV 2020 Datos Abiertos · SCITEL XLSX · ENOE tabulados · CE 2024 (H001A).SECCIÓN 3: SETUP DEL ENTORNO DE COMPILACIÓN (WSL UBUNTU)codeBash# 1. Herramientas base + red de seguridad para Python muy reciente (3.13+)sudo apt update && sudo apt install -y python3-pip python3-dev build-essential git osmium-tool tippecanoe jq sqlite3 openjdk-21-jre openjdk-21-jdk gdal-bin nodejs npm# 2. MapShapersudo npm install -g mapshaper# 3. Planetiler (latest, con logging obligatorio de versión)sudo wget -O /usr/local/bin/planetiler.jar https://github.com/onthegomap/planetiler/releases/latest/download/planetiler.jarPLANETILER_VERSION_ACTUAL=$(java -jar /usr/local/bin/planetiler.jar --version 2>&1 | head -n1)echo "?? Planetiler descargado: ${PLANETILER_VERSION_ACTUAL} — registrar en el Libro de Firmas."# 4. go-pmtiles (pineado)cd /tmpwget https://github.com/protomaps/go-pmtiles/releases/download/v1.22.1/go-pmtiles_1.22.1_Linux_x86_64.tar.gztar -xzf go-pmtiles_1.22.1_Linux_x86_64.tar.gzsudo mv pmtiles /usr/local/bin/ && sudo chmod +x /usr/local/bin/pmtiles# 5. Librerías Python (shapely/geopandas pineados)pip install httpx "httpx[http2]" h2 duckdb pyarrow "shapely>=2.0.2,<3.0" "geopandas>=0.14,<1.0" pandas numpy tqdm rich xarray netcdf4 mapbox_vector_tile matplotlib scipy mercantile osmnx unidecode inflect pyogrio pyproj openpyxl psutil --break-system-packages# 6. Motor 'depot'cd ~ && rm -rf depotgit clone https://github.com/Subway-Builder-Modded/depot.gitcd depot && pip install . --break-system-packagesSECCIÓN 4: PROTOCOLO DE INTERACCIÓN PASO A PASOFASE 0: INICIALIZACIÓN, METADATOS Y ZONIFICACIÓN TERRITORIALSolicita al usuario:Nombre de la Metrópoli / Región y Código de Ciudad (3 letras).BBOX [min_lon, min_lat, max_lon, max_lat].¿El BBOX cruza más de una entidad federativa? Listar claves involucradas.Perfil de Granularidad (1/2/3).¿Colonias mapeadas como polígonos administrativos (admin_level=10)?Zonificación y Protocolo de POIs Manuales:El censo DENUE topa a las empresas masivas en el estrato más alto (asumido como 450-500 empleos). Para evitar que mega-fábricas o centros de altísima atracción queden subrepresentados, la IA debe ejecutar el siguiente flujo iterativo:Consulta inicial: Preguntar al usuario si desea definir POIs manualmente.Propuesta de Zonas: Si el usuario acepta, la IA dividirá el mapa en zonas funcionales pequeñas y pedirá aprobación o ajustes sobre esta división.Revisión Iterativa: La IA y el usuario avanzarán zona por zona, proponiendo y definiendo POIs críticos (ej. Hospitales, universidades, plazas, centros financieros, clusters hoteleros, y fábricas masivas).Reglas de Etiquetado: Únicamente los aeropuertos y universidades llevan prefijo (AIR_ y UNI_). El resto de POIs se etiquetan con IDs claros sin prefijos.Generación de Código: Una vez aprobados los POIs de una zona (o de todas), la IA debe imprimir obligatoriamente la lista de POIs en un bloque de código JSON plano (Array de objetos), listo para que el usuario lo copie. Formato requerido:codeJSON[  {"id": "AIR_Aeropuerto", "loc": [-86.874, 21.036], "jobs": 15000, "radius_deg": 0.025, "mode": "MAX"},  {"id": "UNI_CampusNorte", "loc": [-86.840, 21.150], "jobs": 8000, "radius_deg": 0.008, "mode": "BOOST"},  {"id": "Plaza_Mayor", "loc": [-86.840, 21.150], "jobs": 6000, "radius_deg": 0.010, "mode": "MAX"}]?? CHECKPOINT 0: confirmar parámetros y POIs de todas las zonas antes de Fase 1.?? CHECKPOINT 0.5: AUDITORÍA DE RADIOS DE ABSORCIÓN Y MODOS DE POIsPrefijo	Radio Default (°)	~metros	JustificaciónAIR_	0.025	~2,775	Aeropuertos completosUNI_	0.008	~888	Campus universitarios(otro)	0.0075	~833	Fallback genéricoPresentar tabla resuelta (incluyendo el mode de cada POI) y preguntar por sobrescrituras manuales. ?? Ningún POI pasa a Fase 2 sin confirmación.FASE 1: CARTOGRAFÍA BASE ENRIQUECIDAPREGUNTAR AL USUARIO SEGÚN SU PREFERENCIA A INCLUIR_COLONIAS_ADMINISTRATIVAS = {{INCLUIR_COLONIAS_ADMINISTRATIVAS}} SI FALSE OR TRUEScript Maestro: construir_mapa.pycodePythonimport osimport shutilimport globimport psutilfrom depot.maps import MapGenCODIGO_CIUDAD = "{{CODIGO_CIUDAD}}"BBOX = {{BBOX}}ETIQUETAS_CITIES = ['city', 'borough', 'town']ETIQUETAS_SUBURBS = ['city', 'borough', 'town', 'suburb', 'quarter']ETIQUETAS_NEIGHBORHOODS = [    'city', 'borough', 'town', 'suburb', 'quarter',    'neighbourhood', 'neighborhood', 'subdivision',    'village', 'hamlet', 'locality',    'allotment', 'residential', 'city_block', 'isolated_dwelling']INCLUIR_COLONIAS_ADMINISTRATIVAS = {{INCLUIR_COLONIAS_ADMINISTRATIVAS}}def main():    print(f"=== Compilando Mapa Oficial para {CODIGO_CIUDAD} con Toponimia Enriquecida ===")    cores_disponibles = max(1, (os.cpu_count() or 4) - 1)    try:        ram_disponible_mb = int(psutil.virtual_memory().available / (1024 * 1024))        ram_asignada_mb = max(2048, int(ram_disponible_mb * 0.75))    except Exception:        ram_asignada_mb = 6000    print(f"-> Hardware asignado: {cores_disponibles} Cores CPU | {ram_asignada_mb} MB RAM (sobre disponible, no total)")    work_dir = os.path.abspath(".")    native_build_dir = os.path.expanduser(f"~/build_{CODIGO_CIUDAD.lower()}")    pbf_candidatos = glob.glob(os.path.join(work_dir, "*.osm.pbf"))    if not pbf_candidatos:        raise FileNotFoundError("Coloca el archivo regional .osm.pbf (Geofabrik) en este directorio.")    osm_pbf_orig = pbf_candidatos[0]    osm_pbf_name = os.path.basename(osm_pbf_orig)    print(f"-> Archivo OSM detectado: {osm_pbf_name}")    print("-> Preparando entorno de compilación en Linux nativo (ext4)...")    if os.path.exists(native_build_dir):        shutil.rmtree(native_build_dir)    os.makedirs(native_build_dir, exist_ok=True)    shutil.copyfile(osm_pbf_orig, os.path.join(native_build_dir, osm_pbf_name))    m = MapGen(        city=CODIGO_CIUDAD, bbox=BBOX, osmpbf=osm_pbf_name,        RAM=ram_asignada_mb, ncores=cores_disponibles,        cities=ETIQUETAS_CITIES, suburbs=ETIQUETAS_SUBURBS,        neighborhoods=ETIQUETAS_NEIGHBORHOODS, build_dir=native_build_dir    )    print("-> Extrayendo geometrías, vialidades, colonias, aeropuertos y edificios 3D...")    m.run_all()    print("\n-> Trasladando archivos generados al directorio de trabajo...")    output_dir = os.path.join(native_build_dir, CODIGO_CIUDAD)    archivos_salida = [        f"{CODIGO_CIUDAD}.pmtiles", "buildings_index.bin.gz", "roads.geojson",        "runways_taxiways.geojson", "ocean_depth_index.json.gz"    ]    for arch in archivos_salida:        src = os.path.join(output_dir, arch)        if os.path.exists(src):            shutil.copyfile(src, os.path.join(work_dir, arch))            print(f"    ? {arch} listo.")    print(f"\n? ¡Mapa {CODIGO_CIUDAD} generado exitosamente!")if __name__ == '__main__':    main()?? CHECKPOINT 1: confirmar <CODIGO_CIUDAD>.pmtiles, roads.geojson, buildings_index.bin.gz generados sin errores.?? CHECKPOINT 1.5-A: INSUMOS MACROECONÓMICOS DECLARADOSPresentar y aprobar: TIL_1 crudo por entidad, tabla H001A por municipio, factores de crecimiento, catálogo de POIs con radios y modos confirmados, umbral de muestra mínima (default 500).Confirmación de POIs Externos: Si el usuario decidió mapear POIs personalizados en la Fase 0, la IA DEBE detenerse aquí y preguntarle explícitamente si ya tiene listo su bloque final de POIs (exportado desde su herramienta visual). Prohibido avanzar a la Fase 2 o imprimir generar_demanda.py hasta que el usuario pegue la lista definitiva de POIs para inyectarla en el código.FASE 2: MOTOR DE DEMANDA VECTORIZADOScript Maestro: generar_demanda.pycodePythonimport os, glob, json, math, sysimport numpy as npimport pandas as pdimport geopandas as gpdfrom shapely.geometry import Pointfrom shapely.strtree import STRtreefrom shapely.ops import nearest_pointsfrom rich.console import Consolefrom rich.table import Tablefrom rich.panel import Panelfrom rich.progress import trackconsole = Console()# ==============================================================================# 1. PANEL DE CONTROL MACROECONÓMICO Y ESPACIAL# ==============================================================================CODIGO_CIUDAD = "{{CODIGO_CIUDAD}}"BBOX = {{BBOX_DICT}}GRID_SIZE = {{GRID_SIZE}}MIN_RESIDENTS = {{MIN_RESIDENTS}}MIN_JOBS = {{MIN_JOBS}}TIL_1_ESTATAL = {{TIL_1_ESTATAL}}UMBRAL_MUESTRA_MINIMA = {{UMBRAL_MUESTRA_MINIMA}}FACTOR_DEFAULT_ESTATAL = 1 + TIL_1_ESTATALTASA_PEA = {{TASA_PEA}}CE_2024_BENCHMARKS = {{CE_2024_BENCHMARKS_DICT}}FACTORES_CRECIMIENTO_MUN = {{FACTORES_CRECIMIENTO_MUN_DICT}}FACTOR_CRECIMIENTO_DEFAULT = {{FACTOR_CRECIMIENTO_DEFAULT}}MUN_CENTROIDS = {{MUN_CENTROIDS_DICT}}SEED_SIMULACION = 42rng = np.random.default_rng(SEED_SIMULACION)MAX_CELDAS_MATRIZ_EN_MEMORIA = 50_000_000DENUE_ESTRATOS = {    "0 a 5 personas": 2.24, "6 a 10 personas": 7.75, "11 a 30 personas": 18.17,    "31 a 50 personas": 39.37, "51 a 100 personas": 71.41, "101 a 250 personas": 158.90,    "251 y más personas": 450.00}RADIOS_DEFAULT_POR_PREFIJO = {    "AIR_": 0.025,    "UNI_": 0.008}RADIUS_DEFAULT_FALLBACK = 0.0075SPECIAL_POIS = {{SPECIAL_POIS_LIST}}console.print(f"[dim]· Semilla de simulación fijada en {SEED_SIMULACION} — resultados reproducibles.[/dim]")# ==============================================================================# 2. FUNCIONES DE SANEAMIENTO Y CALIBRACIÓN# ==============================================================================def format_cve_mun(cve_mun_raw, cve_ent_raw):    s_mun = str(cve_mun_raw).strip()    s_ent = str(cve_ent_raw).strip().zfill(2)    if not s_mun or s_mun.lower() == 'nan':        return "-1"    if not s_ent or s_ent == '00' or s_ent.lower() == 'nan':        return "-1"    try:        num_mun = int(float(s_mun))        num_ent = int(float(s_ent))        if num_mun < 0 or num_ent < 1 or num_ent > 32:            return "-1"        if len(str(num_mun)) > 3:            return "-1"        return f"{str(num_ent).zfill(2)}{str(num_mun).zfill(3)}"    except Exception:        return "-1"def calibrar_factor_informalidad(cve_mun, df_denue_bbox, ce_benchmarks, til_1, umbral_muestra, factor_default):    if cve_mun not in ce_benchmarks:        return factor_default, "SIN_META_CE2024", "No hay benchmark CE2024 para este municipio"    jobs_formal_mun = df_denue_bbox.loc[df_denue_bbox['cve_mun_clean'] == cve_mun, 'jobs_formal'].sum()    if jobs_formal_mun < umbral_muestra:        return factor_default, "MUESTRA_INSUFICIENTE", f"Solo {int(jobs_formal_mun)} empleos formales (< {umbral_muestra})"    meta_ce2024 = ce_benchmarks[cve_mun]['empleos_ce']    if meta_ce2024 <= 0:        return factor_default, "META_INVALIDA", "H001A registrado como 0 o negativo"    factor_bruto = meta_ce2024 / jobs_formal_mun    techo_teorico = 1.0 / (1.0 - til_1)    piso_teorico = 1.0    if factor_bruto > techo_teorico:        return techo_teorico, "CLAMPED_TECHO", f"Factor bruto {factor_bruto:.3f} excede techo {techo_teorico:.3f}"    if factor_bruto < piso_teorico:        return piso_teorico, "CLAMPED_PISO", f"Factor bruto {factor_bruto:.3f} por debajo de 1.0"    return factor_bruto, "CALIBRADO", f"Calibrado contra H001A={meta_ce2024:,}"def resolver_radio_poi(poi_id, radius_manual=None):    if radius_manual is not None:        return radius_manual, "MANUAL"    for prefijo, radio in RADIOS_DEFAULT_POR_PREFIJO.items():        if poi_id.startswith(prefijo):            return radio, f"DEFAULT_{prefijo.strip('_')}"    return RADIUS_DEFAULT_FALLBACK, "FALLBACK_GENERICO"for poi in SPECIAL_POIS:    radio, origen = resolver_radio_poi(poi["id"], poi.get("radius_deg"))    poi["radius_deg_resuelto"] = radio    poi["radius_origen"] = origen    poi["mode"] = poi.get("mode", "MAX").upper()    poi["denue_jobs_absorbidos"] = 0.0console.print(Panel(f"[bold cyan]SUBWAY BUILDER MÉXICO v5.2.0[/bold cyan]\n[white]Compilando Demanda Calibrada Vectorizada para [bold yellow]{CODIGO_CIUDAD}[/bold yellow][/white]", expand=False))# ==============================================================================# 3. INGESTA DENUE — MULTI-ESTADO REAL# ==============================================================================denue_candidates = [f for f in glob.glob("*.csv") if "denue" in f.lower()]if not denue_candidates:    console.print("[bold red]? Error:[/bold red] No se encontró el archivo CSV del DENUE.")    sys.exit(1)denue_file = denue_candidates[0]console.print(f"[green]?[/green] Ingestando DENUE: [cyan]{denue_file}[/cyan]")df_denue = Nonefor enc in ['utf-8-sig', 'latin1', 'utf-8', 'ISO-8859-1']:    try:        df_denue = pd.read_csv(denue_file, encoding=enc, low_memory=False, dtype=str)        df_denue.columns = [c.strip().lower() for c in df_denue.columns]        break    except Exception:        continueif df_denue is None:    console.print("[bold red]? Error:[/bold red] No fue posible leer el DENUE con ninguna codificación soportada.")    sys.exit(1)if 'cve_ent' not in df_denue.columns:    console.print("[bold red]? Error crítico:[/bold red] El DENUE no trae columna 'cve_ent'.")    sys.exit(1)df_denue['lat'] = pd.to_numeric(df_denue['latitud'], errors='coerce')df_denue['lon'] = pd.to_numeric(df_denue['longitud'], errors='coerce')df_denue['cve_mun_clean'] = np.vectorize(format_cve_mun)(df_denue['cve_mun'], df_denue['cve_ent'])df_denue = df_denue[    (df_denue['lon'] >= BBOX["min_lon"]) & (df_denue['lon'] <= BBOX["max_lon"]) &    (df_denue['lat'] >= BBOX["min_lat"]) & (df_denue['lat'] <= BBOX["max_lat"])].dropna(subset=['lat', 'lon']).copy()col_entidad_nombre = 'entidad' if 'entidad' in df_denue.columns else Noneif col_entidad_nombre:    entidades_detectadas = df_denue[col_entidad_nombre].dropna().unique()    if len(entidades_detectadas) > 1:        console.print(Panel(f"[bold yellow]?? BBOX MULTI-ESTADO DETECTADO:[/bold yellow] {', '.join(entidades_detectadas)}", style="yellow"))    elif len(entidades_detectadas) == 1:        console.print(f"[green]?[/green] BBOX mono-estado confirmado: [cyan]{entidades_detectadas[0]}[/cyan]")col_per = 'per_ocu' if 'per_ocu' in df_denue.columns else 'personal_ocupado'if col_per not in df_denue.columns:    console.print("[bold red]? Error:[/bold red] No se encontró columna de personal ocupado.")    sys.exit(1)df_denue['jobs_formal'] = df_denue[col_per].map(DENUE_ESTRATOS).fillna(2.24)df_denue['ageb_clean'] = df_denue['ageb'].astype(str).str.strip().str.upper().str.replace('-', '').str.zfill(4)df_denue['mza_clean'] = pd.to_numeric(df_denue['manzana'], errors='coerce').fillna(-1).astype(int).astype(str)# ==============================================================================# 4. CALIBRACIÓN EMPÍRICA DE INFORMALIDAD# ==============================================================================console.print("\n[yellow]? Calibrando FACTOR_INFORMALIDAD por municipio contra CE 2024...[/yellow]")factores_calibrados = {}tabla_calibracion = Table(title=f"Calibración Empírica de Informalidad ({CODIGO_CIUDAD})")tabla_calibracion.add_column("Cve", style="cyan")tabla_calibracion.add_column("Municipio", style="white")tabla_calibracion.add_column("DENUE Formal", justify="right", style="yellow")tabla_calibracion.add_column("H001A (CE24)", justify="right", style="green")tabla_calibracion.add_column("Factor", justify="right", style="bold")tabla_calibracion.add_column("Estado", style="magenta")municipios_en_bbox = [m for m in df_denue['cve_mun_clean'].unique() if m != "-1"]for cve in municipios_en_bbox:    factor, estado, detalle = calibrar_factor_informalidad(        cve, df_denue, CE_2024_BENCHMARKS, TIL_1_ESTATAL, UMBRAL_MUESTRA_MINIMA, FACTOR_DEFAULT_ESTATAL    )    factores_calibrados[cve] = factor    nombre_mun = CE_2024_BENCHMARKS.get(cve, {}).get('nombre', '(sin nombre en catálogo)')    jobs_f = df_denue.loc[df_denue['cve_mun_clean'] == cve, 'jobs_formal'].sum()    meta = CE_2024_BENCHMARKS.get(cve, {}).get('empleos_ce', 0)    color_estado = "green" if estado == "CALIBRADO" else "yellow"    tabla_calibracion.add_row(cve, nombre_mun, f"{int(jobs_f):,}", f"{meta:,}" if meta else "—", f"{factor:.3f}", f"[{color_estado}]{estado}[/{color_estado}]")console.print(tabla_calibracion)df_denue['factor_informalidad_aplicado'] = df_denue['cve_mun_clean'].map(factores_calibrados).fillna(FACTOR_DEFAULT_ESTATAL)df_denue['jobs'] = df_denue['jobs_formal'] * df_denue['factor_informalidad_aplicado']# ==============================================================================# 5. CENSO CPV 2020 — FORMATO DATOS ABIERTOS AGEB URBANA# ==============================================================================censo_csvs = [f for f in glob.glob("*.csv") if "denue" not in f.lower()]censo_xlsxs = [f for f in glob.glob("*.xlsx") if "denue" not in f.lower()]dfs_censo = []if censo_xlsxs:    console.print(f"[green]?[/green] Ingestando Censo desde Excel: [cyan]{censo_xlsxs[0]}[/cyan]")    d = pd.read_excel(censo_xlsxs[0], dtype=str)    d.columns = [c.strip().upper() for c in d.columns]    dfs_censo.append(d)elif censo_csvs:    console.print(f"[green]?[/green] Ingestando Censo desde CSV: [cyan]{censo_csvs}[/cyan]")    for cf in censo_csvs:        for enc in ['utf-8-sig', 'latin1', 'utf-8']:            try:                d = pd.read_csv(cf, encoding=enc, low_memory=False, dtype=str)                d.columns = [c.strip().upper() for c in d.columns]                if len(d) == 30000:                    console.print(Panel("[bold yellow]?? ADVERTENCIA:[/bold yellow] " + cf + " tiene exactamente 30,000 filas (límite SCITEL web).", style="yellow"))                dfs_censo.append(d)                break            except Exception:                continueelse:    console.print("[bold red]? Error:[/bold red] No se encontró archivo del Censo.")    sys.exit(1)df_censo = pd.concat(dfs_censo, ignore_index=True)columnas_requeridas = ['ENTIDAD', 'MUN', 'POBTOT', 'P_15YMAS', 'AGEB', 'MZA']faltantes = [c for c in columnas_requeridas if c not in df_censo.columns]if faltantes:    console.print(f"[bold red]? Error:[/bold red] Faltan columnas: {faltantes}")    sys.exit(1)df_censo['cve_mun_clean'] = np.vectorize(format_cve_mun)(df_censo['MUN'], df_censo['ENTIDAD'])df_censo['pobtot_num'] = pd.to_numeric(df_censo['POBTOT'], errors='coerce').fillna(0)df_censo['pob15ymas_num'] = pd.to_numeric(df_censo['P_15YMAS'], errors='coerce').fillna(0)df_censo['mza_num'] = pd.to_numeric(df_censo['MZA'], errors='coerce').fillna(0)df_censo['ageb_clean'] = df_censo['AGEB'].astype(str).str.strip().str.upper().str.replace('-', '').str.zfill(4)filas_antes = len(df_censo)df_censo = df_censo[(df_censo['mza_num'] > 0) & (df_censo['pobtot_num'] > 0)].copy()filas_perdidas = filas_antes - len(df_censo)console.print(f"[dim]· Filas de agregados y manzanas con POBTOT=0 excluidas: {filas_perdidas:,}[/dim]")df_censo['mza_clean'] = df_censo['mza_num'].astype(int).astype(str)df_censo['factor'] = df_censo['cve_mun_clean'].map(FACTORES_CRECIMIENTO_MUN).fillna(FACTOR_CRECIMIENTO_DEFAULT)df_censo['pobtot_num'] = df_censo['pobtot_num'] * df_censo['factor']df_censo['pob15ymas_num'] = df_censo['pob15ymas_num'] * df_censo['factor']# ==============================================================================# 6. IMPUTACIÓN JERÁRQUICA Y RESPALDO POR CENTROIDE# ==============================================================================mza_coords = df_denue.groupby(['cve_mun_clean', 'ageb_clean', 'mza_clean'])[['lon', 'lat']].mean().reset_index()ageb_coords = df_denue.groupby(['cve_mun_clean', 'ageb_clean'])[['lon', 'lat']].mean().reset_index().rename(columns={'lon': 'lon_ageb', 'lat': 'lat_ageb'})df_censo_geo = pd.merge(df_censo, mza_coords, on=['cve_mun_clean', 'ageb_clean', 'mza_clean'], how='left')df_censo_geo = pd.merge(df_censo_geo, ageb_coords, on=['cve_mun_clean', 'ageb_clean'], how='left')df_censo_geo['lon'] = df_censo_geo['lon'].fillna(df_censo_geo['lon_ageb'])df_censo_geo['lat'] = df_censo_geo['lat'].fillna(df_censo_geo['lat_ageb'])sin_geo = df_censo_geo['lon'].isna()if sin_geo.sum() > 0:    for mun_id, center in MUN_CENTROIDS.items():        mask_mun = sin_geo & (df_censo_geo['cve_mun_clean'] == mun_id)        n_puntos = mask_mun.sum()        if n_puntos > 0:            radio = 0.040            lons_rand = center[0] + rng.normal(0, radio * 0.4, n_puntos)            lats_rand = center[1] + rng.normal(0, radio * 0.4, n_puntos)            df_censo_geo.loc[mask_mun, 'lon'] = np.clip(lons_rand, BBOX["min_lon"] + 0.01, BBOX["max_lon"] - 0.01)            df_censo_geo.loc[mask_mun, 'lat'] = np.clip(lats_rand, BBOX["min_lat"] + 0.01, BBOX["max_lat"] - 0.01)df_censo_geo = df_censo_geo[    (df_censo_geo['lon'] >= BBOX["min_lon"]) & (df_censo_geo['lon'] <= BBOX["max_lon"]) &    (df_censo_geo['lat'] >= BBOX["min_lat"]) & (df_censo_geo['lat'] <= BBOX["max_lat"])].dropna(subset=['lon', 'lat']).copy()# ==============================================================================# 7. MALLA ESPACIAL Y ABSORCIÓN INTELIGENTE DE POIs# ==============================================================================grid = {}def get_grid_key(lon, lat): return f"{int(math.floor(lon / GRID_SIZE))}_{int(math.floor(lat / GRID_SIZE))}"console.print(f"\n[yellow]? Auditoría de radios de absorción de POIs Especiales:[/yellow]")tabla_radios = Table(title=f"?? RADIOS DE ABSORCIÓN APLICADOS ({CODIGO_CIUDAD})")tabla_radios.add_column("POI ID", style="cyan")tabla_radios.add_column("Radio (m aprox.)", justify="right")tabla_radios.add_column("Origen", style="magenta")for poi in SPECIAL_POIS:    r_m = int(poi["radius_deg_resuelto"] * 111000)    tabla_radios.add_row(poi["id"], f"~{r_m:,} m", poi["radius_origen"])console.print(tabla_radios)absorbidos_count = 0for _, r in df_denue.iterrows():    poi_coincidente = None    for poi in SPECIAL_POIS:        r_deg = poi.get("radius_deg_resuelto", RADIUS_DEFAULT_FALLBACK)        if math.hypot(r['lon'] - poi["loc"][0], r['lat'] - poi["loc"][1]) <= r_deg:            poi_coincidente = poi            break    if poi_coincidente is not None:        poi_coincidente["denue_jobs_absorbidos"] += r['jobs']        absorbidos_count += 1        continue    k = get_grid_key(r['lon'], r['lat'])    if k not in grid:        grid[k] = {"lons": [], "lats": [], "jobs": 0.0, "residents": 0.0, "pea_15ymas": 0.0}    grid[k]["lons"].append(r['lon']); grid[k]["lats"].append(r['lat']); grid[k]["jobs"] += r['jobs']console.print(f"[green]?[/green] Registros DENUE absorbidos en POIs Especiales: [yellow]{absorbidos_count:,}[/yellow]")for _, r in df_censo_geo.iterrows():    k = get_grid_key(r['lon'], r['lat'])    if k not in grid:        grid[k] = {"lons": [], "lats": [], "jobs": 0.0, "residents": 0.0, "pea_15ymas": 0.0}    grid[k]["lons"].append(r['lon']); grid[k]["lats"].append(r['lat'])    grid[k]["residents"] += r['pobtot_num']    grid[k]["pea_15ymas"] += r['pob15ymas_num']# ==============================================================================# 8. STRtree — CRS VALIDADO + SNAPPING BATCH + RESOLUCIÓN DE POIs# ==============================================================================roads_gdf = gpd.read_file("roads.geojson")if roads_gdf.crs is None:    console.print(Panel("[bold red]?? roads.geojson no declara CRS.[/bold red] Se asumirá WGS84.", style="red"))elif roads_gdf.crs.to_epsg() != 4326:    console.print(Panel(f"[bold red]? CRS INCOMPATIBLE:[/bold red] {roads_gdf.crs}. Reproyectando...", style="red"))    roads_gdf = roads_gdf.to_crs(epsg=4326)else:    console.print("[green]?[/green] CRS de roads.geojson confirmado: EPSG:4326")road_geoms = [g for g in roads_gdf.geometry if g is not None and not g.is_empty]tree = STRtree(road_geoms)celdas_validas = [(k, cell) for k, cell in grid.items() if not (cell["jobs"] < MIN_JOBS and cell["residents"] < MIN_RESIDENTS)]puntos_grid = np.array([Point(float(np.mean(cell["lons"])), float(np.mean(cell["lats"]))) for _, cell in celdas_validas])console.print(f"[yellow]? Snapping vial vectorizado para {len(puntos_grid):,} puntos de demanda...[/yellow]")demand_points = []if len(puntos_grid) > 0:    indices_nearest = tree.query_nearest(puntos_grid)    for idx, ((k, cell), pt, nearest_idx) in enumerate(zip(celdas_validas, puntos_grid, indices_nearest)):        lon, lat = pt.x, pt.y        nearest_road = road_geoms[nearest_idx]        if pt.distance(nearest_road) > 0.0004:            proj_pt = nearest_points(pt, nearest_road)[1]            lon, lat = proj_pt.x, proj_pt.y        demand_points.append({            "id": f"dp_{idx+1:04d}",            "location": [round(lon, 5), round(lat, 5)],            "jobs": int(round(cell["jobs"])),            "residents": int(round(cell["residents"])),            "pea_15ymas": int(round(cell.get("pea_15ymas", 0.0))),            "popIds": []        })if len(SPECIAL_POIS) > 0:    tabla_poi_auditoria = Table(title=f"?? AUDITORÍA DE ABSORCIÓN Y FUSIÓN DE POIs ({CODIGO_CIUDAD})")    tabla_poi_auditoria.add_column("POI ID", style="cyan")    tabla_poi_auditoria.add_column("Modo", style="magenta")    tabla_poi_auditoria.add_column("Manual", justify="right", style="yellow")    tabla_poi_auditoria.add_column("DENUE Absorbido", justify="right", style="blue")    tabla_poi_auditoria.add_column("Final Asignado", justify="right", style="bold green")    tabla_poi_auditoria.add_column("Diagnóstico", style="white")    puntos_poi = np.array([Point(poi["loc"][0], poi["loc"][1]) for poi in SPECIAL_POIS])    indices_poi_nearest = tree.query_nearest(puntos_poi)        for poi, nearest_idx in zip(SPECIAL_POIS, indices_poi_nearest):        manual = int(poi["jobs"])        denue_abs = int(round(poi["denue_jobs_absorbidos"]))        modo = poi["mode"]                if modo == "MAX":            final_jobs = max(manual, denue_abs)            diag = "[green]OK (Compensación)[/green]" if manual >= denue_abs else "[bold red]Ajustado a piso DENUE[/bold red]"        elif modo in ["ADDITIVE", "BOOST"]:            final_jobs = manual + denue_abs            diag = "[cyan]Suma (Demanda Exógena)[/cyan]"        elif modo == "REPLACE":            final_jobs = manual            diag = "[yellow]Sobrescritura forzada[/yellow]"        else:            final_jobs = max(manual, denue_abs)            diag = "Fallback MAX"        poi["jobs_resueltos"] = final_jobs        tabla_poi_auditoria.add_row(poi["id"], modo, f"{manual:,}", f"{denue_abs:,}", f"{final_jobs:,}", diag)        pt = Point(poi["loc"][0], poi["loc"][1])        nearest_road = road_geoms[nearest_idx]        loc = poi["loc"]        if pt.distance(nearest_road) > 0.0004:            proj_pt = nearest_points(pt, nearest_road)[1]            loc = [round(proj_pt.x, 5), round(proj_pt.y, 5)]                demand_points.append({            "id": poi["id"],            "location": loc,            "jobs": int(poi["jobs_resueltos"]),            "residents": 0,            "pea_15ymas": 0,            "popIds": []        })        console.print(tabla_poi_auditoria)# ==============================================================================# 9. MODELO GRAVITATORIO VECTORIZADO CON BATCHING DE MEMORIA# ==============================================================================origins = [p for p in demand_points if p["residents"] >= MIN_RESIDENTS]destinations = [p for p in demand_points if p["jobs"] >= MIN_JOBS]n_origenes, n_destinos = len(origins), len(destinations)tamano_matriz = n_origenes * n_destinosconsole.print(f"\n[yellow]? Simulando matriz O-D ({n_origenes:,} orígenes × {n_destinos:,} destinos = {tamano_matriz:,} celdas)...[/yellow]")if n_destinos == 0 or n_origenes == 0:    console.print("[bold red]? Error:[/bold red] Orígenes o destinos insuficientes para construir la matriz O-D.")    sys.exit(1)if tamano_matriz > MAX_CELDAS_MATRIZ_EN_MEMORIA:    batch_size = max(1, MAX_CELDAS_MATRIZ_EN_MEMORIA // n_destinos)    n_batches = math.ceil(n_origenes / batch_size)    console.print(Panel(f"[bold yellow]?? Matriz excede techo seguro.[/bold yellow] Procesando en {n_batches} bloques de ~{batch_size:,} orígenes.", style="yellow"))else:    batch_size = n_origenes    n_batches = 1dest_coords = np.array([d["location"] for d in destinations])dest_jobs = np.array([d["jobs"] for d in destinations], dtype=float)lon2, lat2 = np.radians(dest_coords[:, 0]), np.radians(dest_coords[:, 1])ids_d = np.array([d["id"] for d in destinations])pops = []pop_id_seq = 1for batch_idx in track(range(n_batches), description="Procesando bloques O-D y asignando viajes..."):    inicio = batch_idx * batch_size    fin = min(inicio + batch_size, n_origenes)    origins_batch = origins[inicio:fin]    orig_coords = np.array([o["location"] for o in origins_batch])    lon1, lat1 = np.radians(orig_coords[:, 0]), np.radians(orig_coords[:, 1])    ids_o_batch = np.array([o["id"] for o in origins_batch])    dlat = lat2[np.newaxis, :] - lat1[:, np.newaxis]    dlon = lon2[np.newaxis, :] - lon1[:, np.newaxis]    a = np.sin(dlat / 2.0)**2 + np.cos(lat1[:, np.newaxis]) * np.cos(lat2[np.newaxis, :]) * np.sin(dlon / 2.0)**2    dist_matrix = 6371.0 * 2.0 * np.arcsin(np.clip(np.sqrt(a), 0.0, 1.0))    job_weight = dest_jobs[np.newaxis, :] ** 0.85    friction = np.exp(-0.040 * dist_matrix)    weights = job_weight * friction    weights[dist_matrix > 70.0] = 0.0    self_mask = ids_o_batch[:, None] == ids_d[np.newaxis, :]    weights[self_mask] = 0.0    for i_local, o in enumerate(origins_batch):        w_row = weights[i_local]        sum_w = np.sum(w_row)        if sum_w <= 0: continue        probs = w_row / sum_w        non_zero_idx = np.where(w_row > 0)[0]        if len(non_zero_idx) == 0: continue        n_sample = min(len(non_zero_idx), 10)        p_sub = probs[non_zero_idx] / np.sum(probs[non_zero_idx])        chosen_sub = rng.choice(len(non_zero_idx), size=n_sample, p=p_sub, replace=False)        chosen_indices = non_zero_idx[chosen_sub]        commuters = int(o["pea_15ymas"] * TASA_PEA)        chosen_weights = probs[chosen_indices]        sub_probs = chosen_weights / np.sum(chosen_weights)        for k_idx, c_idx in enumerate(chosen_indices):            dest = destinations[c_idx]            d_val = float(dist_matrix[i_local, c_idx])            size = max(3, int(commuters * sub_probs[k_idx]))            while size > 0:                chunk = min(size, 150)                pid = f"pop_{pop_id_seq:06d}"                dist_m = int(d_val * 1000 * 1.35)                pops.append({                    "id": pid, "size": chunk, "residenceId": o["id"], "jobId": dest["id"],                    "drivingSeconds": max(300, int((dist_m / 1000.0) / 48.0 * 3600)),                    "drivingDistance": max(1000, dist_m)                })                o["popIds"].append(pid); dest["popIds"].append(pid); pop_id_seq += 1; size -= chunk    del dist_matrix, weights, job_weight, friction, a# ==============================================================================# 10. CONSERVACIÓN DE MASA POR ORIGEN# ==============================================================================console.print("\n[yellow]? Aplicando reescalado de conservación de masa por origen...[/yellow]")pops_por_origen = {}for p in pops:    pops_por_origen.setdefault(p["residenceId"], []).append(p)total_ajustes = 0for o in origins:    presupuesto_real = int(o["pea_15ymas"] * TASA_PEA)    lista_pops = pops_por_origen.get(o["id"], [])    if not lista_pops:        continue    total_asignado = sum(p["size"] for p in lista_pops)    if total_asignado == 0 or presupuesto_real == 0:        continue    factor_ajuste = presupuesto_real / total_asignado    if abs(factor_ajuste - 1.0) > 0.01:        total_ajustes += 1    for p in lista_pops:        p["size"] = max(1, round(p["size"] * factor_ajuste))console.print(f"[dim]· Orígenes reescalados para respetar presupuesto PEA real (base P_15YMAS): {total_ajustes:,} de {len(origins):,}[/dim]")# ==============================================================================# 11. GARANTÍA DE FLUJOS INBOUND PARA DESTINOS# ==============================================================================destinos_sin_viajes = [d for d in destinations if len(d["popIds"]) == 0]console.print(f"\n[yellow]? Generando flujos de respaldo para {len(destinos_sin_viajes):,} destinos sin viajes entrantes...[/yellow]")pasajeros_relleno = 0for d in destinos_sin_viajes:    d_coord = np.array(d["location"])    d_lon, d_lat = np.radians(d_coord[0]), np.radians(d_coord[1])    orig_lats = np.radians(np.array([o["location"][1] for o in origins]))    orig_lons = np.radians(np.array([o["location"][0] for o in origins]))    d_dlat = orig_lats - d_lat    d_dlon = orig_lons - d_lon    d_a = np.sin(d_dlat / 2.0)**2 + np.cos(d_lat) * np.cos(orig_lats) * np.sin(d_dlon / 2.0)**2    d_dist = 6371.0 * 2.0 * np.arcsin(np.clip(np.sqrt(d_a), 0.0, 1.0))    cercanos_idx = np.argsort(d_dist)[:3]    for idx_o in cercanos_idx:        o = origins[idx_o]        d_val = float(d_dist[idx_o])        size_relleno = 5        pid = f"pop_{pop_id_seq:06d}"        dist_m = int(d_val * 1000 * 1.35)        pops.append({            "id": pid, "size": size_relleno, "residenceId": o["id"], "jobId": d["id"],            "drivingSeconds": max(300, int((dist_m / 1000.0) / 48.0 * 3600)),            "drivingDistance": max(1000, dist_m),            "categoria": "relleno_conectividad"        })        o["popIds"].append(pid); d["popIds"].append(pid); pop_id_seq += 1        pasajeros_relleno += size_rellenoconsole.print(f"[dim]· Pasajeros de relleno (conectividad, NO respaldados por censo): {pasajeros_relleno:,}[/dim]")# ==============================================================================# 12. TEST DE INTEGRIDAD DE ESQUEMA PRE-EXPORTACIÓN# ==============================================================================console.print("\n[yellow]? Ejecutando test de integridad de esquema (PEA = Población Total)...[/yellow]")errores_integridad = 0for p in demand_points:    if p.get("pea_15ymas", 0) > p.get("residents", 0):        errores_integridad += 1if errores_integridad > 0:    console.print(f"[bold red]? ALERTA DE INTEGRIDAD:[/bold red] {errores_integridad} puntos con pea_15ymas > residents. Revisar pipeline de ingesta censal antes de continuar.")else:    console.print("[green]?[/green] Test de integridad de esquema (PEA = Población Total) superado.")# ==============================================================================# 13. EXPORTACIÓN Y RESUMEN FINAL (Checkpoint 1.5-B / Checkpoint 2)# ==============================================================================with open("demand_data.json", "w", encoding="utf-8") as f:    json.dump({"points": demand_points, "pops": pops}, f, separators=(',', ':'))total_pasajeros_censales = sum(p['size'] for p in pops if p.get('categoria') != 'relleno_conectividad')total_pasajeros_relleno = sum(p['size'] for p in pops if p.get('categoria') == 'relleno_conectividad')total_pasajeros = total_pasajeros_censales + total_pasajeros_rellenoresumen_panel = f"""[bold green]? DEMANDA TOTAL CALIBRADA GENERADA EXITOSAMENTE[/bold green][cyan]• Puntos de Demanda Activos:[/cyan] {len(demand_points):,}[cyan]• Cohortes de Viajes (pops):[/cyan] {len(pops):,}[bold yellow]• Pasajeros Censales (respaldados por CPV+PEA):[/bold yellow] {total_pasajeros_censales:,}[dim]• Pasajeros de Relleno (conectividad técnica, no censales):[/dim] {total_pasajeros_relleno:,}[bold white]• TOTAL (usar en config.json ? population):[/bold white] {total_pasajeros:,}"""console.print(Panel(resumen_panel, style="green"))?? CHECKPOINT 1.5-B: presenta tabla_calibracion, tabla_poi_auditoria, el resultado del test de integridad (Sección 12) y el desglose censal vs. relleno. Requiere aprobación explícita.?? CHECKPOINT 2: solicita al usuario la cifra exacta de Pasajeros Censales (no la suma con relleno) para usar en config.json.FASE 3: METADATOS Y SINCRONIZACIÓN (config.json)codeJSON{  "name": "{{NOMBRE_COMPLETO_CIUDAD}}",  "code": "{{CODIGO_CIUDAD}}",  "description": "{{DESCRIPCION_CORTA_MENOR_A_80_CARACTERES}}",  "population": {{TOTAL_PASAJEROS_CENSALES}},  "initialViewState": {    "zoom": {{ZOOM_INICIAL}},    "latitude": {{CENTRO_LAT}},    "longitude": {{CENTRO_LON}},    "bearing": 0  },  "creator": "{{CREADOR}}",  "version": "5.2.0"}?? CHECKPOINT 3: confirmar config.json guardado en la raíz junto a los demás archivos.FASE 4: EMPAQUETADO E INSTALACIÓNSeleccionar los 7 archivos: config.json, demand_data.json, buildings_index.bin.gz, roads.geojson, runways_taxiways.geojson, <CODIGO_CIUDAD>.pmtiles, ocean_depth_index.json.gz.Comprimir en ZIP plano: <CODIGO_CIUDAD>.zip.Abrir Kronifer's Map Manager / Railyard ? ADD A MAP.?? CHECKPOINT 4: confirmar carga correcta, burbujas únicas y limpias en aeropuertos/universidades, y 60 FPS estables.FASE 5: AUDITORÍA FINAL, GOBERNANZA Y REGISTRO EN GUESTBOOKPresentar Resumen de Mejoras y Observaciones de la sesión.Solicitar Aprobación explícita del usuario.Firmar el Guestbook (Sección 6) solo tras aprobación afirmativa.SECCIÓN 5: RESOLUCIÓN DE ERRORES Y MATRIZ DE RENDIMIENTO5.1 Matriz de Errores Geoespaciales y de CómputoError / Síntoma	Causa Raíz	Solución ObligatoriaBurbuja gigante sobre carretera/parque industrial	POI con residents > 0	Fijar residents: 0 y pea_15ymas: 0 (Directiva 6)Municipios enteros sin población	Filtro por lista blanca en vez de BBOX puro	Filtro estrictamente espacial (Directiva 7)0.0% cobertura CE2024 en municipios de un segundo estado	cve_mun resuelto contra entidad global fija	format_cve_mun(cve_mun, cve_ent) con entidad real (Directiva 8/9)Demanda sistemáticamente baja en todas las ciudades	FACTOR_INFORMALIDAD como tasa cruda < 1.0 aplicada directo	Calibración empírica (Directiva 4-bis)Commuters/población no cambian pese a ajustar TASA_PEA	pea_15ymas extraído pero nunca propagado	Verificar que grid, demand_points y commuters usen pea_15ymas, no residentspopulation cambia entre corridas idénticas	Sin semilla determinística unificada	rng = np.random.default_rng(SEED) (Directiva 19)Total de pasajeros no coincide con pea_15ymas × TASA_PEA	Piso max(3,...) y relleno sin descuento de presupuesto	Reescalado de conservación de masa (Directiva 14)Terminal tarda minutos/horas pese a "estar vectorizado"	Bucle for anidado oculto en operación auxiliar (self-match)	Vectorizar toda operación N×M (Directiva 10)OOM en mega-metrópolis	Matriz O-D monolítica sin techo de memoria	Procesamiento por bloques (Directiva 11)Burbujas encimadas / empleo duplicado en aeropuertos	Radio de absorción fijo insuficiente	Radios por categoría de prefijo (Directiva 16, Checkpoint 0.5)Empleo total de una zona cae drásticamente tras declarar un POI	poi["jobs"] manual inferior al DENUE real en la zona	Revisar tabla_poi_auditoria. Usar modo MAX con piso automático sobre datos censales.Faltan colonias o localidades rurales	Etiquetas place=* restrictivas	ETIQUETAS_NEIGHBORHOODS extendida (Directiva 17)Demanda muy baja (<150k en 1M hab)	Archivo SCITEL truncado a 30k filas	Descargar .xlsx completo o CSV Datos Abiertosexit status 108 / mmap in decode	Compilación sobre NTFS /mnt/c/	Compilar en ~/build_<ciudad>/ (ext4 nativo)Fallo de compilación en Python muy reciente (3.13+)	Faltan wheels precompilados	Instalar python3-dev y build-essentialDescripción desborda en el juego	>80 caracteres en config.json	Reducir "description" (Directiva 18)5.2 Ratios de Empleo Sectorial para POIs EspecialesSector	Métrica Base	Ratio	EjemploHotelería All-Inclusive	Habitación/Llave	1.1–1.6 empleos/hab	Resort 3,000 habs ? 3,500–4,800 empleosComunidad Aeroportuaria	Pasajeros Anuales	1/900–1,100 pax	32M pax ? ~32,000 empleosCampus Universitario	Alumnos+Docentes	1 matrícula = 1 viaje/día	3,500 alumnos ? UNI_ con 3,500Hospital Alta Especialidad	Camas	1.5–2.2 empleos/cama	250 camas ? ~2,600 empleosNodos Portuarios	Cruces Diarios	Gravitatorio	2,500–6,000 empleos virtuales5.3 Matriz de Rendimiento (con validación real)Tarea	Enfoque Obsoleto	Enfoque v5.2.0	GananciaCálculo O-D Gravitatorio	Bucle Python (42 min)	NumPy vectorizado (1 seg)	~2500×Self-Match O-D	Bucle anidado (~132.7s medido, ThinkPad T14)	Broadcasting booleano (0.83s medido, ThinkPad T14)	~160× (validado en hardware real)Snapping Vial	Loop punto a punto sobre STRtree	query_nearest batch	~15× adicionalCompilación de Mapa	Cores/RAM fijos	Dinámico sobre RAM disponible	~2.5× sin sobre-comprometer memoriaMatriz O-D Mega-Metrópoli	Monolítica (riesgo OOM)	Por bloques con liberación explícita	Escalable sin límite teóricoSimulación V8	Chunks <50 (>150k pops)	Chunks 150–200 + conservación de masa	60 FPS, cifras auditablesFin del archivo maestro Subway_Builder_AI_Copilot_Master_v5.1.3.md